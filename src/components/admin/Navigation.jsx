function Navigation({ title = "Admin Dashboard" }) {

    return (
        <header className="navigation">

            <h2>
                {title}
            </h2>

        </header>
    );
}

export default Navigation;
